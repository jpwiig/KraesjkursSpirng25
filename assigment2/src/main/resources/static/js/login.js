document.addEventListener("DOMContentLoaded", () => {
    console.log("READY!")
    /*
     The form should send a POST request to the /login API endpoint with the username and
password.
In Spring Boot, validate the credentials:
• If the username is "admin" and the password is "12345", redirect the user to the /verification
page.
• If the credentials are incorrect, display an error message stating "Username or password is
incorrect."
     */
    let user = {
        //todo: live demo on how to get data from here
        username: "bob",
        password: "alice"

    }
})

async function checkCredentials(){

}